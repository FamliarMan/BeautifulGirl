apply plugin: 'com.android.library'

dependencies {
}

android {
    compileSdkVersion 27

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 22
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }

    }

}
